{
  "name": "goelorides",
  "version": "1.0.0",
  "private": true,
  "description": "GoëloRides — site statique pour sorties vélo",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
